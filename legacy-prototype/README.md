# Legacy Prototype

This directory contains the original interactive prototype (pre-React implementation) that the main application in `/src` was converted from. Kept for historical reference only — not part of the build system.

To view the prototype, open `Slush Rematch.dc.html` in a browser (requires the DC runtime via `support.js`).
